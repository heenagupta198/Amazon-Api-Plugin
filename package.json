{
  "name": "amazon-api-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Amazon Product Advertising API plugin development workspace",
  "scripts": {
    "dev": "node src/server.js",
    "test": "node --test test/*.test.js",
    "lint": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
